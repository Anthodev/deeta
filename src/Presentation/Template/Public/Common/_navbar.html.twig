<div class="navbar bg-base-100 shadow-sm fixed-top z-10">
    <div class="navbar-start"></div>
    <div class="navbar-end">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="{{ path('app_admin_index') }}" class="btn btn-sm btn-dash btn-primary">Dashboard</a>
        {% else %}
            <a href="{{ path('app_admin_login') }}" class="btn btn-sm btn-dash btn-primary">Se connecter</a>
        {% endif %}
    </div>
</div>
